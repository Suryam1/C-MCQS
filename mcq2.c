#include <stdio.h>
int main(){
    // int i=0;
    // if(i++)
    // printf("%d",i++);
    // else if(i)
    // printf("%d",++i);
    // else;
    // printf("%d",i);


    float a=2.3;
    if(a==2.3)
    printf("A");//Here a is float and 2.3 is double so they are not equal
    printf("B");
}